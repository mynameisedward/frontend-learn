Функция внутри useEffect может вызываться дважды

Это из за того, что включен StrictMode (строгий режим)

Это нормально и если сделать build проекта(npm run build) то все будет нормально 
    и функция будет вызываться один раз

    