1. submit формы делаю на onSubmit

2. в localStorage данные хранятся в строке JSON
    Перед тем как передать данные на localStorage нужно преобразовать их в строку 
            JSON.stringify()

    А если хотим взять данные с localStorage, нужно спарсить строку
            JSON.parse()


3. В initialState в useState можно передать функцию, которая вычислить состояние
    Вызовется при первом рендере компонента

