1. Сгенерировать случайное число от L до R(не включительно)

    1. Math.random() 
        Вернет от 0 до 1(не включительно)

    2. Умножаем Math.random() на (R - L)

    3. Сдвиг
        + L

    4. Math.floor()
        Полученное округлить вниз



2. Взять любое число от 0 до N
    Math.floor(Math.random() * N)



3. Задача перемешать элементы массива
    1. Идем с конца массива 
    На каждой итерации:
        А. Берем случайный индекс j
            j = случайный индекс от 0 до i
            const j = Math.floor(Math.random() * (i + 1))

        Б. Меняем arr[i] на arr[j]  





