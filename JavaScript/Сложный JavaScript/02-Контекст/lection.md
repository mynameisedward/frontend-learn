                     Контекст

Ключевое слово this указывает на тот объект, в контексте которого было вызвано слово this

Можно вызывать метод bind у функции, чтобы привязать контекст ее выполнения к какому либо объекту

