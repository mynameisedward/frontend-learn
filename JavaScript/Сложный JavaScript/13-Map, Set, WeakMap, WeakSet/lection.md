---------------- MAP ------------------------

Object.entries(obj)
    Создает массив, состоящий из массивов ключ-значение из объекта obj

Object.fromEntries(entries)
    Делает все наоборот 



const map = new Map([
    ['name', 'Edward'],
    ['age', 17],
    ['status', 'student'],
])
Map принимает entries

Методы я описал в map.js


            Отличие объектов от Map
1. Любые типы данных в Map могут быть ключами
    в объекте так нельзя
    с помощью map.set() мы можем создать новый ключ
    зачем? не понятно








---------------- SET ------------------------
new Set() принимает массив значений






---------------- WEAKMAP ------------------------
Это Map, но с помощью него мы можем избежать различных утечек данных

Ключами могут являться только объекты

Есть только методы set, get, delete, has
Других нету





---------------- WEAKSET ------------------------
Значениями являются только объекты

Если какой либо объект удаляется сборщиком мусора, то он удаляется из WEAKSET

Может говорить только о том, есть ли объект
Только метод has





weak значит слабый


