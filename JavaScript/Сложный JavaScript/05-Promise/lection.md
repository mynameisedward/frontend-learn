Promise - объект, который принимает функцию, которая принимает две функции: resolve и reject
    объект Promise выполняет какой-то асинхронный код 

Функция resolve вызывается когда успешно закончена асинхронная операция

После можно написать then(), куда передается функция которая выполняется после того, как промис завершился
    после resolve()
        Принимает 

Помимо then, есть еще метод catch(), который срабатывает в случае ошибки, после 
    после reject()

Еще есть метод finally(), который будет выполняться в любом случае


Как правило, все асинхронные действия должны возвращать промис